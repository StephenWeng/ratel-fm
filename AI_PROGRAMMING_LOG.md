# AI 编程对话追溯记录

> 本文档用于记录 ratel-fm 项目中 AI 编程协作的关键对话、需求变更、处理结果和后续追溯线索。
>
> 维护规则：
> 1. 每次 AI 处理项目需求后，在本文档追加一条记录。
> 2. 每条记录至少包含：时间、需求来源、处理内容、处理结果、验证情况、遗留事项。
> 3. 涉及数据库、初始化数据、配置、打包脚本、前端交互、权限、日志、AI 助手等变更时，需要在记录中明确影响范围。
> 4. 历史未完整记录的对话，可按“历史导入”方式整理摘要，并标明来源文件。

## 记录模板

```markdown
## YYYY-MM-DD HH:mm

- 需求来源：
- 处理内容：
- 处理结果：
- 验证情况：
- 遗留事项：
```

## AI 编程执行原则

### 1. 对话与处理结果记录原则

- 以后每次进行 AI 编程对话和编码处理时，都必须先遵循本文档中的 AI 编程执行原则。
- 以后每次 AI 编程对话，以及每次处理结果的简要概述，都必须记录到本文档。
- 记录内容至少包含：需求来源、处理内容、处理结果、验证情况、遗留事项。
- 涉及数据库、初始化数据、配置、打包脚本、前端交互、权限、日志、AI 助手、智能检索、审批流等变更时，必须明确影响范围。
- 如果一次处理包含多项需求，应按模块或主题拆分记录，方便后续追溯。

### 2. 编码原则

- 源码必须保持可交接、可审计、可快速定位业务意图，不能让业务规则只存在于开发人员记忆中。
- 编码结构要先抽取稳定业务接口，再按供应商、存储介质或运行环境拆分实现类。例如本地视觉模型和云端视觉模型必须实现同一识别接口，由策略服务按优先级编排，避免业务入口直接写多个供应商分支。
- 多处复用的路径校验、文本归一化、错误原因提取、模型可用性检查等逻辑应抽取公共方法或公共服务，不能在 Controller、Service 或前端页面中复制粘贴。
- 前后端不同模块按目录隔离，例如 web、service、repository、domain、config 等均按模块分目录。
- 数据库字段命名使用下划线，例如 `created_time`、`modify_time`；代码使用驼峰命名。
- 新增数据库字段、索引、约束或初始化数据时，必须同步维护 PostgreSQL 和 H2 初始化逻辑，并更新 `src/main/resources/init.sql`。

### 3. 注释原则

- 后续每次新增或修改代码时，必须同步维护注释。
- 后端 Java：实体类、枚举、DTO、配置类、Repository、Service、Controller、校验器、工具类均需要类级注释。
- 后端字段：实体字段、DTO 组件、常量、成员变量必须说明字段含义、来源、业务用途和关键限制；实体字段还需要尽量同步维护 Hibernate `@Comment`。
- 后端方法：公开方法和关键私有方法必须使用 Javadoc 说明方法目的，并用步骤说明主要实现流程，例如查询、校验、转换、持久化、记录审计日志。
- 前端 TypeScript/Vue：`interface`、`type`、`const`、`let`、响应式变量、计算属性、表单对象字段、工具函数、事件处理函数都需要说明用途。
- 前端方法：涉及查询、保存、导出、状态流转、附件、AI 跳转、校验的函数必须说明整体目的和执行步骤。
- 注释要解释“为什么有这个字段/方法”和“主要业务步骤”，不能只重复变量名。
- 表单保存、状态流转、库存扣减、凭证过账、AI 检索、流水快照等核心流程必须在方法注释中写清步骤。
- Getter、Setter、简单映射方法也应有注释，至少说明读取或写入的业务字段。
- 新增数据库字段时，必须同步维护实体字段注释、数据库字段注释初始化、DTO 字段说明、前端类型字段说明和查看流水展示说明。
- 配置文件也必须维护注释。`application*.yml`、日志配置、Maven assembly 描述符、部署启停脚本需要说明配置用途、默认值来源、运行边界和安全注意事项；JSON、lock 文件等语法不支持注释的配置文件，需要在相邻 README、部署说明或主文档中说明维护口径。
- 工程和代码中应标明开发组织 `ratel`，开发人员 `WenZhang`，联系方式 `18782945613`。

### 4. 打包原则

- 除非用户明确要求打包，否则后续代码修改完成后不默认执行完整打包验证。
- 用户明确要求打包时，需要确保前端类型检查、Vite 生产构建、后端编译、AOT 处理、H2 模板库生成和便携包生成均正常。
- 推荐验证命令：

```powershell
$env:JAVA_HOME='D:\jdk\24.0.1'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
& 'C:\Users\210744552\.vscode\extensions\oracle.oracle-java-26.0.0\nbcode\java\maven\bin\mvn.cmd' -DskipTests package
```

- 交付产物包括：
  - `target/ratel-fm.jar`
  - `target/ratel-fm-portable.zip`
  - `target/ratel-fm-ollama.zip`
  - `target/ratel-fm-qdrant.zip`
- Ollama 独立包的实际模型仓库来源是工程根目录 `ollama-models`，不是 `src/main/ollama-package/models`；新增模型必须先同步到 `ollama-models`，再执行 `build-ollama.bat`。
- 本地 OCR 默认视觉模型为 `qwen2.5vl:7b`，交付前需要确认 `ratel-fm-ollama.zip` 内存在 `models/manifests/registry.ollama.ai/library/qwen2.5vl/7b`。
- 主应用包、Qdrant 包和 Ollama 包不得并行执行 Maven AOT 构建；并行构建会共享 `target/spring-aot` 并产生临时文件冲突。需要按顺序执行主包、Qdrant 包、Ollama 包。
- 构建日志中的 Vite chunk 体积警告、第三方库 PURE 注释警告、H2Dialect 显式配置警告和 npm audit 警告需要记录；只要 Maven 最终 `BUILD SUCCESS`，本次打包产物即可作为当前交付包。
- 鉴于 Ollama 打包耗时较长，除非 Ollama 模型、Ollama 运行包或相关脚本发生变化，否则只需要执行 ratel-fm 主应用打包。

## 2026-07-20 00:00

- 需求来源：用户要求在项目根目录增加 AI 编程对话记录文档，并将 `C:\Users\210744552\Desktop\ratel.fm.txt` 中的不完整历史对话纳入记录。
- 处理内容：
  - 在项目根目录新增本文档 `AI_PROGRAMMING_LOG.md`。
  - 读取历史对话文件 `ratel.fm.txt`。
  - 按主题整理历史需求摘要，作为后续追溯基础。
- 处理结果：
  - 已建立统一追溯文档。
  - 已将历史对话归档为摘要，详见下方“历史导入摘要”。
- 验证情况：
  - 已确认项目根目录存在。
  - 已确认历史对话文件可读取。
- 遗留事项：
  - 后续每次处理需求后，应继续追加新记录。
  - 如需要完整逐字归档，可再将原始 `ratel.fm.txt` 复制或整理为单独附件文档。

## 历史导入摘要

来源文件：`C:\Users\210744552\Desktop\ratel.fm.txt`

## 2026-07-20 17:18

- 需求来源：用户要求删除项目中 `target` 目录下无用文件，以及项目中的无用文件。
- 处理内容：
  - 清理 Maven 构建产物目录 `target`，包括历史 jar、original jar、便携 zip、Ollama zip、Qdrant zip、classes、test-classes、generated-sources、spring-aot 等构建输出。
  - 删除根目录测试运行日志：`open-webui-runtime-test*.log`、`qdrant-*-test*.log`。
  - 删除临时目录：`tmp`、`.tmp`。
  - 清空运行日志目录 `logs` 下的日志文件，保留 `logs` 目录本身，方便后续程序继续写入日志。
  - 删除 `src/main/ollama-package/logs`、`src/main/qdrant-package/logs` 下的运行日志文件，保留运行包目录和依赖文件。
- 处理结果：
  - 已释放历史构建包、临时下载、测试缓存和运行日志占用的空间。
  - 源码目录、前端目录、模型目录、工具目录、文档、配置文件、初始化脚本未删除。
- 验证情况：
  - 已复查 `tmp`、`.tmp` 不存在。
  - 已复查常见 `.log`、`.tmp`、`.bak`、`.old`、`.pid`、`.out`、`.err` 文件未再检出。
  - `target` 曾被工具或 IDE 重新生成空目录，已再次移除。
- 遗留事项：
  - `.git`、`.settings`、`.agents`、`ollama-models` 等目录未处理，因为不能简单判定为无用文件。
  - 如后续需要进一步瘦身模型或运行包，需要按模块确认是否仍需要对应资源。

## 2026-07-20 17:22

- 需求来源：用户要求把 `PA.md` 中的打包、编码、注释原则转移到对话记录文档，并增加“以后每次 AI 编程对话，以及每次处理结果的简要概述记录文档”的原则。
- 处理内容：
  - 在本文档新增“AI 编程执行原则”章节。
  - 增加“对话与处理结果记录原则”，明确以后每次 AI 编程对话和处理结果都必须记录到本文档。
  - 从 `PA.md` 迁入编码原则、注释原则和打包原则。
  - 将 `PA.md` 的 `8.1` 调整为引用本文档，避免同一规则两处维护。
  - 移除 `PA.md` 中原 `8.5 打包验证规范` 的重复内容。
- 处理结果：
  - 编码、注释、打包和 AI 编程记录原则已统一沉淀到本文档。
  - `PA.md` 仍保留工程维护规范入口，但规则正文以本文档为准。
- 验证情况：
  - 已检索确认本文档包含“AI 编程执行原则”“对话与处理结果记录原则”“编码原则”“注释原则”“打包原则”。
  - 已检索确认 `PA.md` 中保留对本文档的引用说明。
- 遗留事项：
  - 后续如果规则继续变化，应优先修改本文档，再按需要在 `PA.md` 中保留简短引用。

## 2026-07-20 17:25

- 需求来源：用户明确要求以后每次进行对话、编码时都要根据原则执行。
- 处理内容：
  - 在“对话与处理结果记录原则”中补充：以后每次进行 AI 编程对话和编码处理时，都必须先遵循本文档中的 AI 编程执行原则。
  - 将本次确认要求追加到本文档。
- 处理结果：
  - 后续对话、编码、验证、打包、记录均以本文档中的 AI 编程执行原则为准。
- 验证情况：
  - 已完成文档更新。
- 遗留事项：
  - 无。

## 2026-07-20 17:33

- 需求来源：用户要求根据现有功能重新生成产品设计文档和技术架构设计文档，并补充产品架构文档中的模块关系、数据流向，以及技术架构文档中的技术架构图、架构设计图。
- 处理内容：
  - 重写 `PA.md`，按当前已有功能整理产品定位、导航、基础信息、登录权限、财务、采购、物流、库存、应收应付、审批、报表、附件、日志、AI 助手等产品设计。
  - 在 `PA.md` 新增“模块关系与数据流向”章节，包含模块关系总览图、基础数据流向表、业务到财务数据流向图、审批数据流向图、附件/日志/知识数据流向表。
  - 新增 `TECH_ARCHITECTURE.md`，整理总体架构、工程结构、后端分层、前端架构、安全、数据、日志、附件、AI、审批、导出、部署、配置和质量要求。
  - 在 `TECH_ARCHITECTURE.md` 新增 Mermaid 技术架构图，包括总体技术架构、后端分层架构、前端架构和部署架构。
- 处理结果：
  - 产品设计文档和技术架构设计文档已按当前项目功能重新生成。
  - 文档中已明确模块之间的业务关系、基础数据流、业务到财务流、审批流和横向支撑数据流。
  - 技术架构文档已包含可渲染的 Mermaid 架构图。
- 验证情况：
  - 已检索确认 `PA.md` 包含“模块关系与数据流向”及相关子章节。
  - 已检索确认 `TECH_ARCHITECTURE.md` 包含“技术架构图”及总体、后端、前端、部署四类架构图。
  - 未执行打包，符合“除非用户明确要求，否则不默认打包”的原则。
- 遗留事项：
  - Mermaid 图需要在支持 Mermaid 的 Markdown 查看器中渲染；普通文本查看器会显示源码。

## 2026-07-20 17:44

- 需求来源：用户要求编写一个 iOS 端功能，可以安装在苹果手机中的程序。
- 处理内容：
  - 基于当前 Windows 开发环境，采用 iOS 可安装 PWA 方案，而不是原生 IPA 方案。
  - 在 `frontend/index.html` 增加 iOS 安装相关 meta、theme-color、manifest 和 apple-touch-icon 声明。
  - 新增 `frontend/public/manifest.webmanifest`，声明应用名称、启动地址、作用域、展示模式和图标。
  - 新增 `frontend/public/service-worker.js`，缓存前端应用壳；明确不缓存 `/ratel/fm/api/` 和 `/ratel/fm/actuator/`，避免业务数据离线过期。
  - 新增 `frontend/src/utils/registerPwa.ts`，在浏览器支持 Service Worker 时注册 PWA。
  - 更新 `frontend/src/main.ts`，接入 PWA 注册。
  - 新增 `IOS_INSTALL_GUIDE.md`，说明 iPhone Safari 添加到主屏幕的安装步骤、使用限制和后续原生 iOS 方案。
  - 更新 `PA.md` 和 `TECH_ARCHITECTURE.md`，补充 iPhone 安装应用和 iOS PWA 架构说明。
- 处理结果：
  - iPhone 用户可通过 Safari 访问 `/ratel/fm/`，使用“添加到主屏幕”安装 `Ratel FM`。
  - 安装后的应用以独立窗口启动，业务功能仍依赖服务器在线。
- 验证情况：
  - 已执行 `npm run build`，前端类型检查和 Vite 构建通过。
  - 已确认 `manifest.webmanifest` 和 `service-worker.js` 输出到 `target/classes/static`。
  - 构建出现 Vite chunk 体积、PURE 注释和动态/静态混合导入警告，未阻断构建。
- 遗留事项：
  - 未生成原生 iOS IPA；如后续需要 App Store 或企业签名安装，需要 Mac、Xcode、Apple Developer 账号和额外 iOS 工程。
  - 若服务器使用 HTTPS 自签名证书，iPhone 需要先信任证书。

## 2026-07-20 17:46

- 需求来源：用户指出前一次理解错误，要求回退 PWA 修改，并明确目标是做一个苹果 App。
- 处理内容：
  - 删除 PWA 新增文件：`frontend/public/manifest.webmanifest`、`frontend/public/service-worker.js`、`frontend/src/utils/registerPwa.ts`、`IOS_INSTALL_GUIDE.md`。
  - 回退 `frontend/index.html` 中的 PWA、Apple Web App、manifest 相关声明。
  - 回退 `frontend/src/main.ts` 中的 PWA 注册代码。
  - 从 `PA.md` 移除“iPhone 安装应用”PWA 章节，并恢复后续章节编号。
  - 从 `TECH_ARCHITECTURE.md` 移除 iPhone Safari/PWA 架构节点和“iOS PWA 安装能力”章节。
  - 删除上次前端构建生成的 `target` 目录，避免遗留 PWA 静态构建产物。
- 处理结果：
  - PWA 方案相关源码、文档和构建产物已回退。
  - 后续方向调整为原生苹果 App，而不是 Safari 添加到主屏幕。
- 验证情况：
  - 已确认 `target`、PWA manifest、Service Worker、PWA 注册文件和 iOS 安装说明文件均不存在。
  - 已确认 `PA.md` 和 `TECH_ARCHITECTURE.md` 不再包含 PWA 章节。
- 遗留事项：
  - 需要基于原生 iOS App 方案继续设计或创建独立 iOS 工程；当前 Windows 环境无法直接完成 Xcode 签名和 IPA 打包。

## 2026-07-22 16:59

- 需求来源：用户截图反馈 AI 组件状态页显示 Ollama 模型服务正常，但 ratel助手对话框提示“AI 模型暂时不可用，未能生成回答”。
- 处理内容：
  - 排查 `AiAssistantService`、`LargeModelRouter`、`OllamaLargeModelProvider`、`OllamaClient` 和 `AiComponentHealthService`。
  - 发现状态页原来只按 provider 级别判断模型服务是否有任意模型可用，未区分聊天、指令、推理等业务场景模型。
  - 修复大模型可用性判断，新增按 `AiModelUseCase` 场景判断当前候选模型是否可用。
  - 修复 ratel助手模型调用失败后的兜底提示，返回真实失败原因，避免只显示泛化的“模型暂时不可用”。
  - 修复 AI 状态页“大模型路由”说明，展示聊天模型、指令模型、推理模型及各自可用状态。
- 处理结果：
  - 状态页能更准确展示不同场景模型是否可用。
  - ratel助手如果模型调用失败，会显示实际失败原因，便于判断是模型未下载、Ollama 超时、熔断、地址或防火墙问题。
- 验证情况：
  - 已执行 `mvn -DskipTests compile`，后端编译通过。
  - Maven 输出存在 settings.xml 非识别标签、npm audit 高危提示、JDK native access/Unsafe 警告，均未阻断编译。
  - 未执行完整打包，符合“除非用户明确要求，否则不默认打包”的原则。
- 遗留事项：
  - 需要重启后端后刷新 AI 状态页，再重新向 ratel助手提问，查看新的具体失败原因。
  - 本次编译生成了 `target/classes` 编译产物，如需保持根目录干净可后续清理 `target`。

## 2026-07-22 17:04

- 需求来源：用户询问“语音控制表单输入的内容”是前端浏览器直接填入，还是走后端大模型处理后填入。
- 处理内容：
  - 核对 `FloatingVoiceCommand.vue`、`voiceCommand.ts`、`voiceRiskPolicy.ts` 中的语音识别、命令解析和表单填入链路。
  - 确认当前语音识别组件拿到最终文本后调用前端 `executeVoiceCommand` 执行规则。
- 处理结果：
  - 当前链路为浏览器 Web Speech API 识别语音文本，前端规则引擎解析命令并直接操作页面 DOM/Element Plus 表单控件。
  - 未调用后端大模型接口处理后再填表；浏览器语音识别本身可能由浏览器厂商能力实现，但不经过 ratel-fm 后端 Ollama/Qwen。
- 验证情况：
  - 已确认表单填入通过 `setNativeValue` 写入 input/textarea 并触发 `input/change` 事件，下拉框和开关也由前端点击或赋值完成。
- 遗留事项：
  - 如果后续需要“大模型理解语音意图后填表”，应新增后端结构化命令解析接口，并保留前端确认和后端业务校验。

## 2026-07-22 17:12

- 需求来源：用户截图反馈 ratel助手浮窗回答区出现横向滚动条，要求不要出现横向滚动条。
- 处理内容：
  - 修改 `frontend/src/components/assistant/FloatingAiAssistant.vue` 中助手浮窗样式。
  - 为面板主体、消息行、消息气泡、引用列表、引用项、引用标题和建议标签补充 `min-width: 0`、`max-width: 100%`、`overflow-wrap: anywhere`、`word-break: break-word` 等约束。
  - 为助手面板主体设置 `overflow-x: hidden`，避免长 Markdown 表格线、英文标识符、URL 或标签内容撑出横向滚动条。
- 处理结果：
  - ratel助手浮窗内容会在面板宽度内自动换行，不再显示横向滚动条。
- 验证情况：
  - 已执行 `npm run build`，前端类型检查和 Vite 构建通过。
  - 构建存在 Rollup PURE 注释、动态/静态混合导入和大 chunk 体积警告，未阻断构建。
- 遗留事项：
  - 前端构建生成了根目录 `target` 产物；尝试清理时递归删除命令被当前工具安全策略拦截，未继续执行删除。

## 2026-07-23 09:00

- 需求来源：用户询问本地知识库上传文件的切片原则、是否会造成上下文缺失、内容重叠度以及每个切片保存的元数据。
- 处理内容：
  - 核对 `LocalKnowledgeDocumentService`、`AiProperties.Knowledge`、`KnowledgeDocument`、`QdrantKnowledgeClient`、`KnowledgeSearchService`、`AiAssistantService`。
  - 确认上传资料先保存源文件，再后台抽取文本或图片 OCR，随后按配置执行字符窗口切片并写入当前选择的向量库。
- 处理结果：
  - 当前配置单片目标长度为 800 字符，相邻切片重叠 120 字符；代码兜底要求单片不低于 300 字符，重叠不超过单片长度的一半。
  - 每个本地知识库切片保存来源类型、来源资料 ID、原文件名、标题、分类、正文切片、摘要、权限码、所属公司、内容哈希、切片序号、embedding 和元数据 JSON。
  - 助手问答默认最多召回 5 条本地知识上下文，进入模型前每条关键内容最多保留 900 字符，因此仍可能在跨多切片、跨章节问题中出现上下文缺失。
- 验证情况：
  - 已完成代码级排查，未修改业务逻辑，未执行打包。
- 遗留事项：
  - 如果需要降低上下文缺失风险，可后续优化为按标题/段落/表格结构切片，增加 chunk 元数据中的前后切片编号，并在检索命中后自动拼接相邻切片。

## 2026-07-23 10:25

- 需求来源：用户要求优化本地知识库上传文件切片策略。
- 处理内容：
  - 修改 `LocalKnowledgeDocumentService` 的本地知识库切片逻辑。
  - 将原来的纯字符窗口切片升级为优先按空行、标题行、表格行等语义边界聚合切片；只有单个段落超过配置切片长度时才回退字符窗口切片。
  - 保留现有 `chunk-size` 和 `chunk-overlap` 配置，继续使用重叠窗口降低跨片断裂风险。
  - 扩展每个切片的 metadata，新增 `chunkIndex`、`totalChunks`、`previousChunkIndex`、`nextChunkIndex`、`sectionTitle`。
- 处理结果：
  - 本地知识库分片更接近章节、段落和表格边界，减少标题与正文、表格行与上下文被硬切开的概率。
  - 检索结果具备相邻切片和章节信息，为后续命中后自动拼接前后切片提供数据基础。
- 验证情况：
  - 已执行 `mvn -DskipTests compile`，后端编译通过。
  - Maven 输出存在 settings.xml 非识别标签、npm audit 高危提示、JDK native access/Unsafe 警告，均未阻断编译。
  - 未执行完整打包，符合“除非用户明确要求，否则不默认打包”的原则。
- 遗留事项：
  - 当前已补充相邻切片元数据，但检索命中后自动追加相邻切片尚未实现。
  - 本次编译生成了 `target/classes` 编译产物，如需保持根目录干净可后续清理 `target`。

## 2026-07-23 10:32

- 需求来源：用户要求单独编写 Markdown 文档，整理工程中与大模型开发相关的知识点，包括多模型门户模式、开发细节、向量索引生成与查询、命中率、召回率和切片。
- 处理内容：
  - 新增 `AI_LLM_DEVELOPMENT_GUIDE.md`。
  - 按总体分层、大模型门户模式、AI 助手问答链路、OCR、多模态识别、本地知识库切片、向量索引生成、向量查询、命中率与召回率、RAG 注意事项、配置项、开发检查清单和改进方向进行整理。
  - 结合当前代码中的 `LargeModelProvider`、`LargeModelRouter`、`KnowledgeVectorStore`、`KnowledgeSearchService`、`LocalKnowledgeDocumentService`、`QdrantKnowledgeClient` 等实际实现描述开发要点。
- 处理结果：
  - 已形成面向后续维护和扩展的大模型开发工程手册。
- 验证情况：
  - 已检查文档章节结构。
  - 未修改业务代码，未执行编译或打包。
- 遗留事项：
  - 后续如新增模型 provider、向量库 provider、prompt 模板或检索策略，应同步维护该文档。

## 2026-07-23 10:38

- 需求来源：用户补充要求大模型开发文档还要覆盖提示词优化、提示词工程和提示词版本管理。
- 处理内容：
  - 在 `AI_LLM_DEVELOPMENT_GUIDE.md` 新增“提示词工程”和“提示词版本管理”两个独立章节。
  - 补充当前提示词分布、提示词分层、RAG 提示词、防幻觉提示词、结构化输出提示词、Query 改写提示词、OCR 提示词、提示词优化方法。
  - 补充 prompt 版本命名、Prompt Registry、变更记录、回归测试、灰度回滚、provider 差异化提示词和安全注意点。
  - 更新开发检查清单和改进方向，加入 prompt ID、版本、回归样例、输出契约和资源化管理要求。
- 处理结果：
  - 文档已覆盖提示词工程从设计、优化、测试到版本治理的完整开发注意事项。
- 验证情况：
  - 已检查 `AI_LLM_DEVELOPMENT_GUIDE.md` 章节编号和新增章节位置。
  - 未修改业务代码，未执行编译或打包。
- 遗留事项：
  - 当前项目提示词仍主要硬编码在 Java 方法中；后续可按文档建议迁移到 `resources/prompts` 并增加版本化加载服务。

## 2026-07-23 10:42

- 需求来源：用户补充希望给出后续开发 Agent 并结合业务的建议。
- 处理内容：
  - 在 `AI_LLM_DEVELOPMENT_GUIDE.md` 新增“业务 Agent 开发建议”章节。
  - 补充 Agent 在 Ratel FM 中的定位、适合优先开发的业务 Agent、分层架构、Tool Calling 设计、风险分级、记忆设计、与采购/物流/库存/应收应付/财务/审批模块的结合方式。
  - 补充 Agent 落地路线、评估指标、审计字段和开发注意点。
  - 更新开发检查清单和后续改进方向，加入受控业务 Agent 框架建议。
- 处理结果：
  - 文档已明确 Agent 应先从只读查询和草稿生成开始，后续再逐步接入受控执行；高风险写操作必须二次确认并走现有后端业务服务。
- 验证情况：
  - 已检查 `AI_LLM_DEVELOPMENT_GUIDE.md` 章节编号和 Agent 章节位置。
  - 未修改业务代码，未执行编译或打包。
- 遗留事项：
  - 后续如正式开发 Agent，建议先设计工具白名单、风险等级、审计表和 prompt 版本记录，再进入代码实现。

## 2026-07-23 10:49

- 需求来源：用户指出 `AI_LLM_DEVELOPMENT_GUIDE.md` 仍不够细，希望在细节处贴出核心代码或伪代码，并明确该文档要让零大模型开发基础的同事也能看懂和照抄。
- 处理内容：
  - 扩展 `AI_LLM_DEVELOPMENT_GUIDE.md`，新增“零基础阅读说明”章节，补充一句话理解、术语表、按任务阅读路径、修改前自查问题和最小验证方法。
  - 在多模型 provider、AI 助手问答、RAG prompt、本地知识库切片、知识索引写入、embedding 生成、query 改写、Qdrant 查询、H2 查询等章节加入核心代码或伪代码。
  - 在新增 provider、助手模式、切片调整、新增知识来源、提示词优化、Agent 工具开发等位置增加“照抄任务卡”，列出要改文件、实现步骤和验收标准。
  - 在提示词版本管理中加入 `PromptTemplateService`、`RenderedPrompt`、prompt 变更表、回归测试和灰度版本选择伪代码。
  - 在业务 Agent 章节加入 Tool 接口、工具注册表、只读工具、写操作待确认、Agent 主流程和二次确认伪代码。
  - 新增“零基础同事常见错误”和“新人开发 AI 功能提交前检查”章节。
- 处理结果：
  - 文档从概念说明扩展为可照抄的工程开发手册，更适合零基础同事按任务推进。
- 验证情况：
  - 已检查文档章节结构，当前包含 17 个一级章节，约 1762 行。
  - 未修改业务代码，未执行编译或打包。
- 遗留事项：
  - 文档中部分代码为伪代码或模板，正式落地时仍需结合当前包名、权限码、DTO 和已有 Service 方法微调。

### 1. 产品定位与总体架构

- 项目定位为财务管理 ERP 系统，覆盖会计科目、凭证记账、采购管理、物流管理、库存台账、应收应付、统计分析、智能检索和 AI 财务助手。
- 要求生成并维护 `PA.md`、`README.md`，描述产品架构、技术架构、模块和功能。
- 后端基础框架要求 Spring Boot、JDK 24、Knife4j 在线接口文档。
- 初期数据库使用 PostgreSQL `fm` 库，后续讨论过 H2 内置数据库、小体量部署和数据初始化兼容。

### 2. 开发规范与注释规则

- 后端、前端、配置文件均要求补充清晰注释。
- 实体类字段、方法、主要逻辑步骤都应有注释，方便后续维护人员理解。
- 工程和代码中标明开发组织 `ratel`，开发人员 `WenZhang`，联系方式 `18782945613`。
- 前后端不同模块按目录隔离，例如 web、service、repository、domain、config 等均按模块分目录。
- 数据库字段命名使用下划线，例如 `created_time`、`modify_time`；代码使用驼峰命名。
- 后续数据库变更需要同步更新 PostgreSQL 和 H2 初始化数据，并维护 `resources/init.sql`。

### 3. 登录、权限与基础信息

- 身份认证采用 JWT，令牌存储登录人基本信息，过期时间 1 小时，距离过期不足半小时自动续期。
- JWT 存放在浏览器 Cookie 中，后端从 Cookie 获取并验证。
- 登录校验包括令牌有效性、人员信息正确性、禁用状态、过期状态。
- 支持同一身份证同一终端类型唯一登录，后登录可选择挤掉前登录。
- 菜单权限覆盖模块、页面、按钮，用户关联角色，角色关联菜单。
- 登录成功和页面刷新后，前端重新获取授权菜单代码并渲染。
- 基础信息逐步调整为字典管理，部门、组织、岗位、项目、公司、物料、客户/供应商、仓库、币种、行政区划等均作为字典或基础数据管理。
- 所属公司作为账套维度，登录时选择所属公司，并用于数据隔离。

### 4. 数据库与初始化数据

- 数据库表和字段要求补齐注释或描述。
- 表结构需要索引，保证查询效率。
- 初始化数据包括菜单、角色、默认账号、会计科目、币种、行政区划等。
- 全国行政区划要求按层级初始化，后续讨论到是否能细化到乡镇。
- 后续引入公司账套后，首页概览、会计科目、凭证记账、采购管理、物流管理、库存台账、应收应付、统计报表、操作日志等需要按所属公司隔离。

### 5. 日志与操作流水

- 系统日志通过日志框架写入文件，关键财务操作需要文件日志记录。
- 日志文件按天压缩，单文件 100M 滚动压缩，保留时间曾从 120 天调整为 10 天。
- 用户关键操作日志记录到数据库表，数据库日志异常不能影响主业务流程。
- 日志管理模块支持查询操作记录，包含账号、身份证、联系方式、部门、操作时间、终端类型、终端标识、模块、功能、参数、结果、响应值等。
- 凭证记账、采购管理、物流管理、库存台账、应收应付等模块要求支持操作流水和右侧悬浮时间轴查看。

### 6. 前端交互与 UI 规则

- 登录页不默认填充 admin，也不提示默认账号。
- 增加 `/login/star` 特殊登录页，夜晚草原、星空、金色星星闪烁、随机流星，并优化家庭人物图片。
- 非登录页直接访问时，需要验证令牌和菜单权限，无效时弹出 10 秒倒计时登录过期提示。
- 不存在路由也要遵循令牌验证逻辑，不直接显示错误 JSON。
- 左侧菜单要按菜单管理中的层级展示。
- Tab 区增加关闭全部按钮，关闭后默认打开已有权限的第一个页面。
- 列表数据较多时表头固定。
- 树型数据默认只展开第一层。
- 表单校验错误提示应在字段下方显示，输入框红色边框，失焦后立即校验。
- 深色主题下多个页面和流程图需要保证可读性。

### 7. 各业务模块需求

- 会计科目：
  - 支持层级树展示。
  - 停用父级时如存在启用下级，需要二次确认。
  - 选择父级时不能选择自身下级，避免嵌套。
  - 初始化说明字段要求补齐。
- 凭证记账：
  - 增加所属年月。
  - 金额保留 8 位小数。
  - 明细行各自选择币种、汇率、人民币金额。
  - 支持生成在线凭证图片并下载、预览。
- 采购管理：
  - 支持项目维度、附件、操作流水、导出、批量删除。
  - 状态流程包括草稿、审批中、已审批、采购中、采购完成、取消采购等。
  - 已收货后可弹出库存台账新增流水。
  - 后续引入流程审批后，采购状态与审批结果联动。
- 物流管理：
  - 状态包括草稿、已创建、已发送、运输中、已送达、已取消等。
  - 状态具有顺序性，不能回退。
  - 修改状态时可编辑实际物流信息并生成流水。
  - 发货和目的行政区划需要存储完整级联关系，搜索支持右 like 查询下级。
- 库存台账：
  - 物料编码和物料名称合并为物料字典选择。
  - 来源仓、目标仓为字典。
  - 出库、调拨数量不能大于库存数量。
  - 增加“物料库存”统计页面，按物料层级统计库存数量。
- 应收应付：
  - 客户/供应商字典选择。
  - 增加收付统计页面，按项目和客户/供应商统计应付、应收、待收、待付金额，并支持导出。

### 8. 导出、附件与文件管理

- 凭证记账、采购管理、物流管理、库存台账、应收应付支持列表导出。
- 如果选择行，则导出选中数据；未选择则按搜索条件导出。
- 导出最大行数由配置控制。
- 多个业务模块支持附件列表，附件可增删改。
- 后端提供统一附件管理，附件保存名称、后缀、大小等信息。
- 文件统一存储到程序运行包下 `files` 目录。
- 引入所属公司后，文件最上层目录按 `所属公司代码_所属公司名称` 区分。
- 文件支持小体量在线预览。

### 9. 打包与部署

- 项目部署目标为 Windows 笔记本，小体量使用。
- 要求安装包自动携带 JDK 等运行依赖，不要求电脑额外安装 JDK。
- 前后端打包到同一个 zip，前端文件放入后端项目中，启动后端即可访问。
- JVM 内存设置为 128M。
- 同时提供 Windows 启动/关闭脚本，以及 Linux sh 启动/关闭脚本。
- 后续增加 Ollama、Qdrant 的独立启动/关闭脚本，以及总启动/关闭脚本。
- 除非用户明确要求，后续代码修改不再默认执行完整打包。
- Ollama 打包较慢，除非 Ollama 有变化，否则只打包 ratel-fm。

### 10. AI 助手、智能检索与模型

- AI 助手和智能检索要求覆盖整个系统上下文，不仅限于会计科目。
- 曾接入千问模型，后续讨论是否改为本地 Ollama 模型，减少外部 API 依赖。
- 讨论 AI 助手是否适合流式输出，结论倾向于流式输出可提升用户体验。
- 增加互联网检索模式需求，希望能像网页版千问、豆包一样检索互联网问答。
- 引入 Qdrant 向量数据库，增加配置开关决定使用 Qdrant 还是 H2 内置向量库。
- Qdrant 需要本地 embedding 模型，不依赖千问 embedding。
- 后续计划下载或接入 DeepSeek-V4-Flash、智谱 GLM-5.2 等模型，用于数学推理、税务系统、银行流水等场景。
- 需要测试智能检索精准度，减少 AI 助手幻觉。

### 11. 审批流程

- 计划引入流程引擎，设立审批中心模块。
- 审批中心包含待办事宜、已办事宜、发起事宜。
- 流程管理按所属公司和功能模块配置流程模板。
- 流程定义支持绘制流程图，并支持按人员、部门、部门加岗位设置审批人。
- 流程查看需要展示流程图、节点状态、流程操作流水和业务表单内容。
- 审批操作保留同意、不同意，必须填写审批意见。
- 采购管理与流程审批联动，提交审批、审批结果、发起采购、已收货、取消采购等均记录流水。

### 12. 最近未闭环问题

- 在另一台电脑启动 Ollama，当前电脑 `telnet 10.105.12.136 11434` 不通，需要排查远程 Ollama 网络监听、防火墙、服务绑定地址等。
- Ollama 和 Qdrant 均部署在其他电脑时，需要确认 ratel-fm 配置中的服务地址、端口、网络连通性和降级策略。
- Qdrant 无法连接时应走 H2 内置向量库，但搜索“青岗”无法命中，需要排查 H2 索引创建、初始化数据入库、分词/embedding、检索字段和检索阈值。

## 2026-07-23 11:20 AI Agent 实际业务开发

- 用户诉求：结合采购、物流、库存、应收应付、财务、审批模块，开发实际可用的业务 Agent。
- 处理结果：
  - 新增 `BusinessAgentDtos`，定义业务 Agent 请求、响应、模块分析结果和证据 DTO。
  - 新增 `BusinessAgentService`，实现第一版只读业务 Agent 编排，覆盖采购、物流、库存、应收应付、财务、审批六个模块。
  - 新增 `BusinessAgentController`，提供 `POST /api/agent/business` 接口，权限要求为 `AI_ASSISTANT_USE`。
  - Agent 会根据问题关键词和指定模块选择分析范围，复用现有业务 Service 查询数据，输出摘要、风险、建议和引用证据。
  - 当前版本严格限制为只读分析，不执行保存、删除、审批、取消、过账等写操作。
- 覆盖的业务检查：
  - 采购：审批中、审批驳回、未生成凭证等风险。
  - 物流：运输中、计划发货日期已过但未送达等风险。
  - 库存：负库存风险，并汇总物料库存统计。
  - 应收应付：待收待付余额、到期逾期风险。
  - 财务：草稿凭证、借贷不平衡风险。
  - 审批：待办事项积压和本人发起流程数量。
- 验证结果：
  - 已执行 `mvn -DskipTests compile`，编译通过。
  - 未执行完整打包，遵循“除非明确要求，不默认完整打包”的原则。
  - 编译会生成 `target` 目录，属于 Maven 正常产物。
- 后续待增强：
  - 前端增加业务 Agent 入口和结果展示页面。
  - 引入大模型 planner/tool-calling，让模型生成调用计划，但后端仍执行白名单工具。
  - 增加 Agent 审计表，记录请求、工具调用、结果摘要和用户确认信息。
  - 高风险写操作后续必须采用“预览 + 用户确认 + 审计”的机制。

## 2026-07-23 11:35 AI Agent 四阶段、安全开关和自检

- 用户诉求：
  - 按照“只读 Agent -> 草稿 Agent -> 受控执行 Agent -> 多步骤 Agent”的步骤继续开发。
  - 增加配置开关控制是否启用 Agent，默认开启。
  - 关键 Agent 增加自检环节，确保准确度和安全性。
- 处理结果：
  - `BusinessAgentRequest` 增加 `stage` 字段，支持 `readOnly`、`draft`、`controlled`、`multiStep`。
  - `BusinessAgentResponse` 增加 `stage`、`actions`、`selfChecks` 字段。
  - 新增 `BusinessAgentAction`，用于返回草稿动作、受控执行计划和多步骤计划。
  - 新增 `BusinessAgentSelfCheck`，用于返回权限边界、证据约束、写操作阻断和阶段合法性自检结果。
  - `BusinessAgentService` 接入 `AiProperties`，读取 `app.ai.agent.enabled` 和 `app.ai.agent.self-check-enabled`。
  - `application.yml` 新增：
    - `app.ai.agent.enabled: ${FM_AI_AGENT_ENABLED:true}`
    - `app.ai.agent.self-check-enabled: ${FM_AI_AGENT_SELF_CHECK_ENABLED:true}`
  - `stage=draft` 时只生成草稿动作计划，不写数据库。
  - `stage=controlled` 时生成受控执行计划，但写操作统一 `executable=false`，等待后续确认令牌、审计表和工具白名单。
  - `stage=multiStep` 时组合草稿动作、受控执行计划和跨模块跟进计划，仍然只返回计划。

## 2026-07-23 16:08

- 需求来源：用户要求阅读项目根目录下的 Markdown 文件，学习已有的产品设计、架构设计，以及编码、打包、注释等原则。
- 处理内容：
  - 阅读根目录 Markdown 文件清单，包括 `README.md`、`PA.md`、`TECH_ARCHITECTURE.md`、`AI_LLM_DEVELOPMENT_GUIDE.md`、`AI_PROGRAMMING_LOG.md`。
  - 梳理产品定位、模块关系、技术分层、前后端工程结构、AI/RAG/Agent 约束、注释规范、表单校验规范、查看流水规范、打包验证原则和 AI 编程追溯规则。
- 处理结果：
  - 已掌握当前项目以“基础信息 + 财务核算 + 业务协同 + 审批中心 + AI 助手”为核心的一体化轻量 ERP 产品设计。
  - 已掌握前后端一体化架构、按模块分目录、统一响应、Cookie JWT、多账套隔离、H2/PostgreSQL 初始化同步、Ollama/Qdrant/H2 知识库配置选择等工程约束。
  - 已明确后续改动需要同步维护注释、前后端校验、数据库初始化、查看流水、知识索引和本追溯记录。
- 验证情况：
  - 本次仅阅读和整理文档，未修改业务代码，未执行编译或打包。
- 遗留事项：
  - 后续具体开发任务开始前，需要结合目标模块源码再次核对现有实现，避免只依据文档推断。

## 2026-07-23 16:23

- 需求来源：用户要求继续开发查询型、对账检查、凭证建议、到期提醒、流程助手、库存风险、经营分析、附件/知识问答等业务 Agent，并按文档类型补充 Agent 内容。
- 处理内容：
  - 扩展 `BusinessAgentRequest`，新增 `agentTypes`，允许前端或调用方指定 Agent 能力类型。
  - 扩展 `BusinessAgentResponse`，新增 `capabilities`，按 Agent 能力返回分析结果、风险、建议、草稿和证据。
  - 新增 `BusinessAgentCapabilityResult`，统一承载各类 Agent 的能力输出。
  - 扩展 `BusinessAgentService`，接入 `CashierService` 和 `KnowledgeSearchService`，在原采购、物流、库存、应收应付、财务、审批模块分析基础上新增 8 类能力：
    - 查询型 Agent。
    - 对账检查 Agent。
    - 凭证建议 Agent。
    - 到期提醒 Agent。
    - 流程助手 Agent。
    - 库存风险 Agent。
    - 经营分析 Agent。
    - 附件/知识问答 Agent。
  - 保持安全边界：`readOnly` 只返回分析结果；`draft` 只生成草稿；`controlled` 和 `multiStep` 只生成 `executable=false` 的受控执行计划。
  - 更新 `PA.md`，补充产品层面的业务 Agent 能力和安全边界。
  - 更新 `TECH_ARCHITECTURE.md`，补充业务 Agent 架构、依赖服务、阶段控制和写操作阻断原则。
  - 更新 `AI_LLM_DEVELOPMENT_GUIDE.md`，补充当前增强版业务 Agent 的请求字段、返回字段、能力类型和执行逻辑。
  - 更新 `README.md`，补充业务 Agent 接口、请求字段、响应字段和维护边界。
- 处理结果：
  - `POST /api/agent/business` 现在可通过 `agentTypes` 指定查询、对账、制证建议、到期提醒、流程助手、库存风险、经营分析和知识问答能力。
  - 响应中同时保留原 `modules` 模块分析结果，并新增 `capabilities` 能力分析结果。
  - Agent 仍不直接保存、删除、审批、取消或过账，后续写操作需要确认令牌、审计表、工具白名单和服务端二次校验后才能放开。
- 验证情况：
  - 已执行 `mvn -DskipTests compile`，编译通过。
  - 首次编译发现采购完成状态枚举应为 `PURCHASE_COMPLETED`，已修正后复验通过。
  - 编译过程中 Maven settings 存在 `public` 标签警告，npm audit 提示 2 个 high severity 漏洞，均为既有环境/依赖提示，本次未处理。
- 遗留事项：
  - 前端尚未增加业务 Agent 独立入口和结果展示页面。
  - 写操作仍未接入确认令牌、Agent 审计表、工具白名单和真正执行链路。
  - 库存低库存阈值当前按 10 的保守规则判断，后续可改为物料字典安全库存字段。

## 2026-07-23 16:32

- 需求来源：用户澄清业务 Agent 总开关语义：关闭时不应让前端展示 Agent 功能，也不应让执行流程调用 Agent，而不是用户点击后后端返回服务不可用。
- 处理内容：
  - `AiComponentStatusResponse` 新增 `agentEnabled` 字段，作为前端判断业务 Agent 是否展示和可调用的统一状态。
  - `AiComponentHealthService` 在 AI 状态中新增 `business-agent` 组件项，展示业务 Agent 开启、关闭和自检状态。
  - `BusinessAgentService` 调整关闭逻辑：`FM_AI_AGENT_ENABLED=false` 时不再抛出服务不可用，而是返回禁用说明、空模块、空能力、空动作，不选择模块、不读取业务证据、不生成 Agent 计划。
  - `frontend/src/types/api.ts` 补充 `agentEnabled` 类型字段。
  - `frontend/src/api/fm.ts` 新增业务 Agent 请求/响应类型、AI 状态短缓存、`businessAgentEnabled()` 和 `runBusinessAgent()`；`runBusinessAgent()` 会先读取 `agentEnabled`，关闭时在前端阻断，不调用 `/api/agent/business`。
  - `AiStatusView.vue` 增加“业务 Agent”状态卡，配置关闭时展示“关闭/入口隐藏，调用阻断”。
  - 同步更新 `PA.md`、`TECH_ARCHITECTURE.md`、`README.md`、`AI_LLM_DEVELOPMENT_GUIDE.md` 中关于 Agent 开关关闭后的行为描述。
- 处理结果：
  - Agent 总开关现在具备产品语义：关闭后前端可以隐藏入口，API 封装不会调用后端 Agent 接口。
  - 即使绕过前端直接调用后端 Agent 接口，后端也只返回禁用说明和空结果，不让 Agent 参与业务分析流程。
- 验证情况：
  - 已执行 `mvn -DskipTests compile`，编译通过。
  - 已执行 `frontend` 下 `npm run build`，`vue-tsc` 和 Vite 生产构建通过。
  - 构建中仍存在既有 npm audit、PURE 注释和 chunk 体积警告，本次未处理。
- 遗留事项：
  - 后续新增业务 Agent 独立页面或按钮时，必须使用 `/api/ai/status` 的 `agentEnabled` 或前端 `api.businessAgentEnabled()` 控制是否渲染。
  - 更新 `AI_LLM_DEVELOPMENT_GUIDE.md`，补充四阶段 Agent、配置开关和关键自检规则。
- 安全边界：
  - 当前版本不会自动执行保存、删除、审批、取消、过账等写操作。
  - 关键写操作必须等后续完成“用户确认 + 服务端二次校验 + Agent 审计记录 + 工具白名单”后才能放开。
- 验证结果：
  - 已执行 `mvn -DskipTests compile`，编译通过。
  - 未执行完整打包。

## 2026-07-23 16:41

- 需求来源：用户要求为业务 Agent 增加前端触发入口：AI 助手页增加“业务 Agent”Tab；采购、库存、应收应付、会计平台页面增加“Agent 分析”按钮；ratel助手识别对账、到期、制证建议等意图时调用 `api.runBusinessAgent()`。
- 处理内容：
  - 新增复用组件 `BusinessAgentPanel.vue`，负责业务 Agent 问题输入、阶段选择、调用 `api.runBusinessAgent()`、展示能力结果、模块结果、自检和动作计划。
  - `AssistantView.vue` 增加“业务 Agent”Tab；ratel助手问答完成后识别对账、到期、制证建议、库存风险等意图，自动切换到业务 Agent 并运行分析。
  - `PurchaseOrdersView.vue`、`InventoryView.vue`、`ArApView.vue`、`AccountingPlatformView.vue` 增加“Agent 分析”按钮和弹窗，并根据当前筛选条件或选中来源生成分析问题。
  - 各页面通过 `api.businessAgentEnabled()` 读取开关，`agentEnabled=false` 时不渲染按钮；`api.runBusinessAgent()` 继续负责关闭时阻断后端 Agent 调用。
  - 更新 `README.md`、`PA.md`、`TECH_ARCHITECTURE.md`、`AI_LLM_DEVELOPMENT_GUIDE.md`，补充前端入口、意图联动、开关语义和当前执行逻辑。
- 处理结果：
  - 业务 Agent 现在可从 AI 助手页、采购、库存、应收应付和会计平台触发。
  - Agent 关闭时前端入口隐藏，封装接口不调用 `/api/agent/business`，符合“关闭后系统展示和执行流程中没有 Agent 功能”的要求。
  - ratel助手可在识别到对账、到期、制证建议、库存风险意图后自动调用业务 Agent 分析。
- 验证情况：
  - 已执行 `frontend` 下 `npm run build`，`vue-tsc` 和 Vite 生产构建通过。
  - Vite 构建仍提示既有 PURE 注释、动态导入和 chunk 体积警告，本次未处理。
- 遗留事项：
  - 物流、出纳、审批中心等页面尚未增加独立“Agent 分析”按钮，可按同一组件模式后续扩展。
  - 写操作仍保持不可执行计划，确认令牌、Agent 审计表和工具白名单尚未开发。
