# Ratel FM 产品设计

[English version](Product-Design.md)

版本日期：2026-07-23

本文档描述产品定位、业务范围、页面行为和交互原则。技术架构见 `技术架构.md`，大模型实现见 `大模型开发知识点整理.md`，编码与打包原则见 `编码打包注释和变更记录.md`。

## 1. 产品定位

Ratel FM 面向小型企业、项目团队、财务和业务协同场景，目标是在低部署成本下完成基础 ERP 和财务闭环。

产品重点：

- 业务单据和财务凭证在同一系统中留痕。
- 采购、物流、库存、应收应付、出纳、凭证之间可以追溯。
- 审批流满足中小团队日常流转，不追求重型 BPM。
- AI 只做查询、解释、检查、建议和草稿，不直接改写业务数据。
- 默认可单机部署，必要时切换 PostgreSQL、Ollama、Qdrant。

## 2. 产品原则

- 账套隔离优先：所有页面、接口、检索、Agent 结果都必须按公司和账套隔离。
- 链路可追溯：业务单据、会计来源、凭证、附件、审批、日志必须能互相定位。
- 人工确认优先：AI 和会计平台只能生成建议或草稿，最终动作由用户确认。
- 权限前后端一致：前端控制展示，后端负责最终校验。
- 错误明确：登录失败、保存失败、审批失败、AI 不可用必须给出可理解提示。
- 低运维负担：主包、Ollama、Qdrant 分开启停，互不拖累。

## 3. 用户角色

| 角色 | 主要工作 |
| --- | --- |
| 系统管理员 | 公司、用户、角色、菜单、基础配置 |
| 财务人员 | 科目、凭证、应收应付、付款、报表、结账 |
| 业务人员 | 采购、物流、收货、库存、附件 |
| 审批人 | 查看单据、审批、驳回、补充意见 |
| 管理者 | 看板、经营分析、风险汇总、Agent 建议 |

## 4. 导航和入口

系统导航按业务域组织：

- 首页：经营看板、待办、风险摘要。
- 基础资料：供应商、客户、项目、物料、部门、人员、仓库。
- 采购管理：采购申请、采购订单、收货相关信息。
- 物流管理：物流单和运输信息。
- 库存管理：库存台账、库存余额、调拨和风险。
- 应收应付：应收、应付、核销、到期风险。
- 出纳管理：付款、收款、流水。
- 会计平台：来源单据、凭证草稿、凭证生成。
- 会计核算：凭证、科目、报表、期间。
- 审批流：流程定义、任务、历史。
- AI 助手：ratel 助手、业务 Agent、本地知识库。
- 系统管理：组织、权限、日志、配置。

业务 Agent 只在 AI 助手页统一展示。采购、库存、应收应付、会计平台页面不再单独展示 `Agent 分析` 按钮。

## 5. 核心业务闭环

采购链路：

```text
采购申请 -> 采购订单 -> 收货/入库 -> 应付 -> 付款 -> 凭证
```

销售和收款链路：

```text
业务来源 -> 应收 -> 收款 -> 核销 -> 凭证
```

库存链路：

```text
期初/入库/出库/调拨 -> 库存台账 -> 库存余额 -> 风险检查
```

审批链路：

```text
单据提交 -> 流程匹配 -> 节点审批 -> 通过/驳回 -> 业务状态更新
```

## 6. AI 产品行为

ratel 助手用于自然语言问答、知识检索、业务解释和入口引导。回答只展示结论和关键依据，不展示大段原始知识片段、召回卡片和内部上下文。

文件存在性问题需要优先回答“有没有文件”，并列出匹配文件名和关键依据。连续追问时需要关联上一轮问题，不应把追问当作全新主题。
结论或关键依据有多条时，助手必须使用数字编号，不使用短横线列表。
思考推理只作为内部过程，用户侧回答不得展示推理草稿、复盘语句或 think 标签。
经营统计、财务报表、对账、账龄、现金流、制证建议、采购、库存、应收应付和审批等财务专业问题，应优先使用实时系统数据和业务 Agent 能力，不应被知识库文件问答带偏。
经营分析类回答应先基于结构化经营指标快照，再由模型归纳表达，保证采购、往来和库存口径一致。

业务 Agent 用于结构化业务分析：

- 查询型 Agent：查单据、库存、应收应付、凭证。
- 对账检查 Agent：检查业务到财务链路是否一致。
- 凭证建议 Agent：生成凭证草稿建议。
- 到期提醒 Agent：识别到期、逾期、未核销。
- 流程助手 Agent：解释审批节点和下一处理人。
- 库存风险 Agent：检查库存异常。
- 经营分析 Agent：汇总经营风险和建议。
- 附件/知识问答 Agent：结合附件、合同、制度和系统数据回答。

关键 Agent 需要自检环节：检查权限、账套、数据来源、金额方向、日期范围、证据完整性和风险提示。

## 7. 配置开关

| 配置 | 默认值 | 行为 |
| --- | --- | --- |
| `FM_AI_ENABLED` | `true` | 控制 AI 总能力 |
| `FM_AI_AGENT_ENABLED` | `true` | 控制业务 Agent |
| `FM_AI_KNOWLEDGE_ENABLED` | `true` | 控制本地知识库 |
| `FM_AI_WEB_SEARCH_ENABLED` | `false` | 控制联网搜索 |

业务 Agent 关闭时：

- 前端不展示业务 Agent Tab。
- ratel 助手不调用业务 Agent。
- 后端接口做兜底拦截。
- 不以“服务不可用”作为主要产品体验，而是让系统表现为没有该功能。

## 8. 交互原则

- 页面优先展示业务结论和可执行动作。
- 表格必须支持滚动，底部内容不能被遮挡。
- 弹窗内表格优先自适应宽度，不能出现无意义的底部横向滚动条；确实字段过多的业务明细应优先通过分组、换行或行编辑降低横向宽度。
- 登录、保存、删除、审批等失败必须展示明确提示。
- 按钮文案要描述动作，不描述技术实现。
- 颜色、边框、阴影、圆角、滚动条和焦点态必须使用统一主题变量，适配浅色、深色、墨绿、金融蓝和 Vue 金主题。
- 当前 UI 基调采用玻璃拟态：承载层使用半透明背景、背景模糊、柔和边框和阴影；输入、表格正文和财务数字必须优先保证可读性，不做过度透明。
- 图标优先使用项目已有开源图标体系，并通过统一玻璃图标容器呈现高光、半透明背景和柔和阴影；不混用授权不清晰的外部图片图标。
- 模块页签、筛选区、表格和统计卡片必须保留稳定内边距，内容不得贴边展示。
- UI 美化优先使用全局主题变量、统一页签容器、表格留白、轻量阴影和状态色，不在单个页面硬编码旧白底灰边样式。
- AI 输出不展示内部 prompt、检索块、原始 JSON、调试字段。
- 加载状态只保留必要提示和按钮转圈，不额外弹出占位卡片。
- AI 组件状态应清晰展示部署排查所需模型信息，包括 Ollama 已配置模型和已下载模型。
- ratel 助手初次打开使用更高的稳定面板高度；思考中隐藏输入框和发送按钮，只保留 `思考中` 与思考动画；回答完成后恢复 `输入问题` 和发送按钮。
- 文件存在性回答必须在结论中列出命中文件；后续总结归纳类追问必须继承上一轮文档主题。

## 9. 非目标

- 不做直接由 AI 自动过账。
- 不做大型集团多组织合并报表。
- 不做完整 BPMN 流程设计器。
- 不做自动银行直连。
- 不承诺离线包包含所有历史模型版本。

## 10. 文档维护要求

出现以下变化时必须同步更新本文档：

- 页面入口变化。
- 业务状态变化。
- Agent 入口、开关或安全边界变化。
- 会计、库存、应收应付等主流程变化。
- 用户手册展示内容变化。

所有文档更新必须保持中英文同步。产品行为变化时，需要同时更新 `Product-Design.md` 和 `产品设计.md`，并检查全部根目录 Markdown，保证 README、技术架构、大模型文档、工程日志和打包文档引用一致。
