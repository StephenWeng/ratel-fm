# Ratel FM 技术架构

[English version](Technical-Architecture.md)

版本日期：2026-07-23

本文档描述系统架构、模块边界、部署包、AI 集成和质量要求。产品行为见 `产品设计.md`，大模型开发细节见 `大模型开发知识点整理.md`。

## 1. 总体架构

Ratel FM 是前后端一体交付应用。前端由 Vue 构建为静态资源，后端 Spring Boot 打包后统一提供页面和 API。

```text
浏览器
  -> Vue 3 前端
  -> HTTP/SSE API
  -> Spring Security/JWT Cookie
  -> Controller
  -> Service
  -> JPA Repository
  -> H2 或 PostgreSQL
```

AI 相关组件：

```text
ratel 助手 / 业务 Agent
  -> 权限和账套上下文
  -> 系统数据 / 附件文本 / 本地知识 / 可选联网搜索
  -> Ollama 或其它模型提供方
  -> 结论、依据、建议、草稿
```

Qdrant 作为可选向量库使用；未启用 Qdrant 时可回退 H2 知识索引。

## 2. 后端分层

- Controller：请求校验、权限注解、DTO 转换、统一响应。
- Service：业务规则、状态流转、日志、Agent 编排。
- Repository：数据库访问，必须带公司和账套条件。
- Entity：持久化模型，字段需要表达业务含义。
- DTO：前端输入输出模型，不能直接暴露内部实体细节。
- Config：安全、AI、附件、数据库、初始化配置。

后端必须承担最终权限校验，不能依赖前端按钮隐藏。

## 3. 前端架构

前端按业务页面组织：

- `views`：页面级组件。
- `components`：复用组件，例如助手、手册、上传、表格。
- `api`：统一 API 包装。
- `stores`：登录用户、权限、菜单、上下文。
- `router`：路由和权限入口。

主题体系由 `stores/theme.ts` 和 `styles/global.css` 共同维护。所有页面应使用 `--surface-color`、`--text-color`、`--primary-color`、`--border-color`、`--shadow-color` 等变量，不直接写固定浅色或深色值。壳布局、常用 Element Plus 控件、滚动条和焦点态在全局层统一适配浅色、深色、墨绿、金融蓝和 Vue 金主题。玻璃拟态变量由 `--glass-surface-color`、`--glass-strong-surface-color`、`--glass-border-color`、`--glass-highlight-color` 和 `--glass-blur` 控制。页签容器、筛选表单、表格单元格、统计卡片和业务分组优先由全局样式提供间距、圆角、阴影、半透明背景和背景模糊。页面级 scoped 样式只处理本模块布局，不应覆盖为固定白底、固定灰边或贴边表格。
图标体系继续复用 Element Plus Icons 和项目品牌 SVG，通过菜单、顶部入口、悬浮助手、语音入口的玻璃图标容器统一表现，不新增来源不明的图片资产。

弹窗内表格由全局样式兜底隐藏横向溢出；业务页面应避免在弹窗内使用固定右列和过大的最小宽度，长文本通过换行或省略处理。

AI 入口统一在 AI 助手页：

- ratel 助手：自然语言对话。
- 业务 Agent：结构化业务分析。
- 本地知识库：上传、索引、检索。

浮窗 ratel 助手保持稳定面板高度：初次打开不再按空内容收缩；检索和推理过程中隐藏问题输入框和发送按钮，只展示 `思考中` 与思考动画。

浮窗发送区通过下拉提供 `思考` / `AI写作` 手动意图选择，默认 `思考`。`思考` 调用 `/api/ai/assistant/stream` 并传入 `reasoning` 模式，由 `AssistantModelRouter` 分配到推理模型场景；前端在等待最终答案时展示阶段化思考过程，最终答案到达后移除该临时消息。`AI写作` 调用 `/api/ai/writing/generate` 生成文件并下载。后端 `AiWritingService` 使用 Apache POI 生成 xlsx、docx、pptx，使用 PDFBox 生成 pdf；采购清单复用 `OperationService.exportPurchaseOrders`，采购按日统计使用当前公司采购单和明细做确定性聚合；经营分析报告通过 `BusinessMetricsService` 读取采购、往来和库存指标后生成，不能落回通用占位文档。

追问检索会把总结、归纳等短请求与最近用户问题和会话摘要合并。知识检索会扩展文档、简历等主题词，避免用户资料被通用系统模块结果挤掉。

助手直答路径和模型提示词都约束输出格式：结论或关键依据有多条时使用数字编号，不使用短横线列表。
助手流式展示和最终响应组装都会清洗模型输出，移除内部推理草稿、复盘语句和 think 标签，再展示给用户或写入会话摘要。
`FinancialIntentTerms` 统一维护财务专业术语，供 ratel 助手路由和业务 Agent 选择复用。问题命中财务专业术语且不是文档/附件问题时，助手跳过本地知识库召回，优先基于实时系统上下文和推理模型回答。

采购、库存、应收应付、会计平台页面不保留独立 `Agent 分析` 按钮。

## 4. 数据架构

默认使用 H2 文件库，便携包首次启动会从模板库复制运行库。正式部署可切换 PostgreSQL。

核心要求：

- 所有业务表包含公司和账套边界。
- 金额字段必须明确方向、币种和精度。
- 状态字段必须和业务动作绑定。
- 附件、审批、凭证、业务单据之间保留来源关系。
- 重要操作写入审计日志和业务时间线。

凭证新增/编辑前端使用在线凭证式录入面板，保存前过滤空白分录，只提交有效分录。后端 `Voucher` 保存客户、部门、业务员、记账人和制单人快照；摘要搜索同时匹配凭证头摘要和分录摘要。在线凭证 Canvas 对无内容行的借方金额、贷方金额区域绘制右上到左下斜线，表示该行无效。

## 5. 安全架构

- 登录使用 JWT Cookie。
- 密码错误、账号禁用、权限不足必须返回明确业务错误。
- 菜单和按钮由权限控制展示。
- 后端接口再次校验菜单、按钮或业务权限。
- 附件访问必须校验所属账套和业务权限。
- AI 检索和 Agent 分析必须使用当前用户上下文。

## 6. AI 架构

AI 模块由以下部分组成：

- 模型提供方：Ollama、预留其它 Provider。
- 模型路由：按问答、指令、推理、embedding 选择模型。
- 助手模型路由器：`AssistantModelRouter` 将操作指令、普通问答和复杂财务推理分配到不同模型场景。
- Prompt 构造器：`AssistantPromptBuilder` 统一维护系统提示词、用户提示词和上下文压缩规则。
- 输出清洗器：`AssistantAnswerSanitizer` 统一清理 think 标签、推理草稿和危险确认措辞。
- 组件健康：展示 Ollama 已配置模型和 `/api/tags` 探测到的已下载模型清单。
- 知识索引：附件、制度、合同、业务摘要。
- 智能检索：关键词、语义、混合检索。
- SSE 输出：对话流式响应。
- 业务 Agent：按任务类型查询数据、检查链路、生成建议。

AI 不直接写业务数据。凭证建议、审批意见、经营建议都必须作为草稿或文本建议返回。

## 7. 业务 Agent 架构

业务 Agent 接口由后端统一接收请求，根据 `agentTypes` 执行不同分析器。

执行步骤：

1. 读取当前用户、公司、账套和权限。
2. 通过 `BusinessAgentSelector` 识别 Agent 类型、业务模块、阶段和查询范围。
3. 查询受控业务数据。
4. 经营类问题先通过 `BusinessMetricsService` 生成结构化经营指标快照，再生成分析结果。
5. 关键 Agent 执行自检。
6. 返回结论、风险、依据和建议。

配置 `FM_AI_AGENT_ENABLED=false` 时，前端入口隐藏，ratel 助手不转交 Agent，后端接口兜底拒绝。

## 8. 部署包架构

交付包分为三类：

| 包 | 内容 | 启停边界 |
| --- | --- | --- |
| 主应用便携包 | Jar、配置、H2 模板库、JDK、主应用脚本 | 只启停 Ratel FM |
| Ollama 独立包 | Windows/Linux Ollama、模型、可选 Open WebUI、脚本 | 只启停 Ollama/Open WebUI |
| Qdrant 独立包 | Windows/Linux Qdrant、Dashboard、数据目录、脚本 | 只启停 Qdrant |

目录按平台区分：

```text
bin/windows
bin/linux
runtime/windows
runtime/linux
```

Linux 脚本要求：

- 使用 `bash`。
- 使用 PID 文件管理进程。
- 使用 `nohup` 后台运行。
- 创建 `logs`、`run`、`storage`、`snapshots` 等目录。
- 启动后做轻量健康检查。
- 不修改系统服务、不写系统目录。

## 9. 打包命令

PowerShell 下 Maven 属性需要加引号，避免 `-D` 参数被错误拆分：

```powershell
$env:JAVA_HOME='D:\jdk\jdk-24.0.1'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
mvn "-Dmaven.compiler.useIncrementalCompilation=false" "-Dmaven.test.skip=true" "-Dspring-boot.aot.skip=true" clean package
```

全量包：

```powershell
mvn "-Dmaven.compiler.useIncrementalCompilation=false" "-Dmaven.test.skip=true" "-Dspring-boot.aot.skip=true" clean package -Pwith-ollama,with-qdrant
```

## 10. 质量要求

- 编译和打包使用 JDK 24。
- 前端构建必须成功。
- 后端编译必须成功。
- 便携包必须包含主应用脚本和内置 JDK。
- Ollama/Qdrant 包必须包含 Windows/Linux 运行时和脚本。
- 日志、模型、运行时、向量数据默认不提交 Git。
- 影响架构的改动必须同步更新中英文技术架构文档，并同步检查全部根目录 Markdown。

## 11. 已知技术债

- 数据库迁移后续应从自动建表迁移到 Flyway 或 Liquibase。
- Prompt 模板后续应资源化、版本化。
- Agent 审计表和确认令牌框架仍需补强。
- 财务、库存、应收应付、审批和 AI 检索需要更多自动化回归测试。
