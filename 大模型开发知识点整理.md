# Ratel FM 大模型开发知识点整理

[English version](LLM-Development-Guide.md)

版本日期：2026-07-23

本文档面向项目开发者，说明 Ratel FM 中大模型、RAG、本地知识、智能检索、OCR 和业务 Agent 的开发原则。

## 1. 核心边界

大模型不能直接操作业务数据。

```text
用户问题 + 权限上下文 + 受控数据
  -> 检索和组装证据
  -> 模型生成结论或草稿
  -> 用户确认或业务服务校验
```

任何写入、过账、付款、审批通过、删除、附件变更，都必须走原有业务接口。

## 2. 基础概念

| 概念 | 项目含义 |
| --- | --- |
| LLM | 用于问答、推理、OCR 解释、草稿生成的模型 |
| Provider | 模型提供方，例如 Ollama |
| Router | 按场景选择模型 |
| Prompt | 给模型的系统和用户指令 |
| RAG | 先检索证据，再让模型回答 |
| Embedding | 文本向量，用于语义搜索 |
| Vector Store | Qdrant 或 H2 知识索引 |
| Chunk | 附件、知识文档、业务摘要切分后的片段 |
| Agent | 围绕明确业务任务的受控分析器 |

## 3. 模型路由

默认模型场景：

| 场景 | 默认模型 | 用途 |
| --- | --- | --- |
| 普通问答 | `qwen2.5:7b` | 中文业务问答、系统解释 |
| 指令识别 | `llama3.2:3b` | 菜单跳转、轻量意图 |
| 复杂推理 | `deepseek-r1:8b` | 风险分析、经营建议 |
| 向量检索 | `bge-m3:latest` | 知识和附件 embedding |

聊天模型可降级到本机已有模型；embedding 模型不能随意降级，因为向量维度和语义空间必须稳定。

AI 组件状态页会同时展示 Ollama 已配置模型和 Ollama `/api/tags` 返回的已下载模型清单，便于区分“已配置但未下载”和“已下载但未被当前场景选中”。

## 4. ratel 助手

ratel 助手负责：

- 回答系统功能问题。
- 检索本地知识和附件。
- 查询可读业务信息。
- 识别是否需要进入业务 Agent。
- 对文件存在性问题给出文件级结论。
- 初次打开浮窗时保持稳定高度；检索和推理过程中隐藏输入框和发送按钮，只展示 `思考中` 与思考动画。

助手内部组件：

- `AssistantModelRouter`：选择问答、指令或推理模型场景。
- `AssistantPromptBuilder`：集中维护 Prompt 和上下文压缩。
- `AssistantAnswerSanitizer`：清理内部推理、think 标签和危险确认措辞。

输出要求：

- 只展示结论和关键依据。
- 不展示检索卡片、长原文片段、内部 JSON、Prompt、调试字段。
- 证据不足时说明限制。
- 连续追问必须结合最近上下文。
- 检索和推理过程中隐藏问题输入框和发送按钮，只展示 `思考中` 与思考动画，回答完成后再恢复输入状态和发送按钮。
- 文件存在性问题需要在结论中列出命中文件名；总结归纳类追问需要继承上一轮文档主题；简历问题需要扩展候选人、工程师、工作经历、项目经历、地点等同义词。
- 结论或关键依据存在多条时，使用 `1、2、3` 数字编号，不使用短横线列表。
- 不展示思维链、推理草稿、复盘语句或 think 标签；流式输出和最终答案都需要清洗。
- 使用统一财务术语表进行意图路由。财务统计和专业分析问题必须优先使用实时系统上下文和推理模型；只有附件、文件、合同、制度、简历、知识库问题才以文档检索为主。

## 5. 文件存在性问答

当用户问“有没有文件”“是否上传过某个文件”“有没有关于某主题的文件”时，回答结构应为：

```text
结论：有/没有/证据不足。
关键依据：匹配到的文件名、主题、片段摘要。
下一步：可继续查看、打开或检索该文件。
```

不得把文件内容摘要当成主要答案，也不得忽略上一轮提到的主题。

## 6. 本地知识库

本地知识库来源：

- 用户上传的制度、合同、文档。
- 附件抽取文本。
- 系统内置说明文档。
- 业务数据摘要。

处理流程：

1. 提取文本。
2. 清洗无效内容。
3. 切分 Chunk。
4. 生成 embedding。
5. 写入 Qdrant 或 H2 知识索引。
6. 检索时按权限和账套过滤。

## 7. 智能检索

检索模式：

- 关键词检索：适合单号、编码、精确名称。
- 语义检索：适合自然语言主题。
- 混合检索：同时使用关键词和语义结果。

搜索结果页面必须有可滚动区域，底部内容不能被页面遮挡。

## 8. OCR 和附件

OCR 只负责文本抽取和结构化建议，不直接生成业务单据。附件文本进入知识库前必须保留附件来源、业务对象、上传人、公司和账套。

## 9. 业务 Agent

业务 Agent 是受控业务分析器，不是自由聊天。

业务 Agent 编排由 `BusinessAgentService` 负责；阶段、模块和 Agent 类型选择由 `BusinessAgentSelector` 负责；经营分析指标由 `BusinessMetricsService` 先结构化，再交给 Agent 生成结论和建议。

当前 Agent 类型：

- 查询型 Agent。
- 对账检查 Agent。
- 凭证建议 Agent。
- 到期提醒 Agent。
- 流程助手 Agent。
- 库存风险 Agent。
- 经营分析 Agent。
- 附件/知识问答 Agent。

统一入口：

- `AI 助手 / 业务 Agent` Tab。
- ratel 助手识别到对账、到期、制证建议、库存风险等意图时，可引导或切换到业务 Agent。
- 业务模块页面不再展示独立 `Agent 分析` 按钮。

## 10. Agent 自检

关键 Agent 必须自检：

- 权限是否允许读取目标数据。
- 公司和账套是否一致。
- 日期范围是否明确。
- 金额方向和币种是否一致。
- 来源单据、库存、应收应付、付款、凭证链路是否完整。
- 结果是否存在证据不足。
- 建议是否可能被误解为自动执行。

自检失败时返回风险说明，不生成确定性结论。

## 11. 配置开关

| 配置 | 默认值 | 说明 |
| --- | --- | --- |
| `FM_AI_ENABLED` | `true` | AI 总开关 |
| `FM_AI_AGENT_ENABLED` | `true` | 业务 Agent 开关 |
| `FM_AI_KNOWLEDGE_ENABLED` | `true` | 本地知识库开关 |
| `FM_AI_WEB_SEARCH_ENABLED` | `false` | 联网搜索开关 |

Agent 关闭后，产品侧应隐藏入口并停止参与流程，后端只做兜底保护。

## 12. 常见错误

- 把召回原文整段展示给用户。
- 回答文件内容，却不回答是否存在文件。
- 忽略上一轮问题。
- 用前端隐藏代替后端权限。
- 让模型生成可直接执行的财务动作。
- embedding 模型变化后不重建索引。
- 关闭 Agent 后仍在业务流程中调用 Agent。

## 13. 验证清单

- AI 助手回答只包含结论和关键依据。
- 文件存在性问题能返回文件名。
- 连续追问能关联上一轮主题。
- Agent 开关关闭后前端入口隐藏。
- Agent 开关关闭后 ratel 助手不转交 Agent。
- 后端接口仍校验 Agent 开关和权限。
- Qdrant 不可用时有明确降级或错误提示。
- AI、RAG、Agent、模型、Prompt、检索或 OCR 相关变更，必须同步更新中英文大模型文档，并同步检查全部根目录 Markdown。
