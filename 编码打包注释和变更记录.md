# Ratel FM 编码、打包、注释和变更记录

[English version](Engineering-Log.md)

版本日期：2026-07-23

本文档记录项目工程原则、打包规则和近期变更。产品设计见 `产品设计.md`，技术架构见 `技术架构.md`，大模型知识见 `大模型开发知识点整理.md`。

## 1. 编码原则

- 修改前先阅读现有代码和根目录文档。
- 按业务模块放置代码，不随意新增扁平公共文件。
- 所有查询和写入必须带公司、账套、权限上下文。
- 前端隐藏只是体验控制，后端必须做最终校验。
- 状态变更必须记录操作日志或业务时间线。
- 新业务字段必须同步实体、DTO、前端类型、表单校验、数据库初始化和日志展示。
- 优先使用项目已有工具类和结构化 API。
- 不做和当前需求无关的重构。

## 2. 注释原则

Java：

- 类注释说明模块职责。
- 字段注释说明业务含义。
- 方法注释说明目的和关键步骤。
- 配置项注释说明默认值、边界和风险。

Vue/TypeScript：

- 复杂状态和流程需要注释。
- 简单赋值、显而易见的模板不写空注释。
- API 类型要表达字段含义。

## 3. 前端原则

- 页面入口和按钮必须符合权限与业务状态。
- 错误要明确展示，尤其是登录、保存、删除、审批。
- 表格和弹窗必须有稳定滚动区域。
- 加载态只保留必要提示，不制造额外遮挡。
- AI 输出不展示内部检索卡片、长原文和调试信息。
- 业务 Agent 统一在 AI 助手页展示，不在业务页面重复放按钮。

## 4. 后端原则

- Controller 不承载复杂业务逻辑。
- Service 负责业务规则、状态流转和日志。
- Repository 查询必须按公司和账套过滤。
- 写接口必须校验权限、状态和幂等风险。
- AI 和 Agent 接口必须校验开关、权限、账套和数据范围。

## 5. 打包原则

- 使用 JDK 24。
- PowerShell 下 Maven `-D` 参数加引号。
- 主应用、Ollama、Qdrant 独立打包和独立启停。
- Windows 和 Linux 脚本按目录区分：`bin/windows`、`bin/linux`。
- Windows 和 Linux 运行时按目录区分：`runtime/windows`、`runtime/linux`。
- 日志、PID、运行库数据、模型仓库默认不提交 Git。

推荐全量打包命令：

```powershell
$env:JAVA_HOME='D:\jdk\jdk-24.0.1'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
mvn "-Dmaven.compiler.useIncrementalCompilation=false" "-Dmaven.test.skip=true" "-Dspring-boot.aot.skip=true" clean package -Pwith-ollama,with-qdrant
```

## 6. Git 原则

不提交：

- `target/`
- `frontend/node_modules/`
- `frontend/dist/`
- `logs/`
- `ollama-models/`
- Ollama/Qdrant runtime、模型、向量库和运行数据
- 本地配置、证书、临时下载目录

需要上传大文件时，应先明确是否使用 Git LFS 或外部制品库。

## 7. 文档维护原则

- 每次产品、架构、AI、打包、编码或交互变更，都必须更新所有受影响的根目录 Markdown 文档。
- 中文版和英文版按成对文档维护，必须在同一次改动中同步更新。
- 每次改动结束前，需要扫描全部根目录 Markdown，保证 README、产品设计、技术架构、大模型文档、工程日志和打包文档引用一致。

## 8. 近期变更记录

### 2026-07-25：凭证记账在线凭证式录入

- 凭证新增/编辑改为在线凭证式所见即所得录入，默认 5 行，保留 `新增分录` 和 `凭证导入`。
- 分录摘要成为行级字段；保存前忽略空白行，有内容行必须完成摘要、科目、借贷金额、币种和汇率校验。
- 凭证主表新增客户、部门、业务员、记账人和制单人快照字段；摘要搜索同时匹配凭证头摘要和分录摘要。
- 在线凭证图片对空白行借方金额和贷方金额区域绘制斜线，表示该行无效。

### 2026-07-25：ratel 助手 AI写作文件生成

- ratel 助手发送区新增 `思考` / `AI写作` 手动意图选择。
- 新增 `/api/ai/writing/generate` 文件生成接口，支持 xlsx、docx、pdf、pptx 下载。
- 新增 `AiWritingService`，使用 Apache POI 生成 Office 文件，使用 PDFBox 生成 PDF。
- 采购清单类 xlsx 复用现有采购导出服务；采购按日统计按当前所属公司、采购日期和明细数量做确定性聚合。

### 2026-07-25：Vue 金主题

- 新增 `vue-gold` 主题配置，主题选择器展示为“Vue 金”。
- 补齐 Vue 金主题的背景、表面、边框、文字、主色、状态色、阴影、焦点环和滚动条变量。
- ratel 助手图标增加视觉居中校正，避免品牌 SVG 在头像容器中偏左偏上。
- 优化页签、筛选区、表格和统计卡片的统一留白，避免内容贴边展示。
- 替换库存、应收应付、审批中心局部硬编码白底灰边样式，统一跟随主题变量。
- 增加玻璃拟态全局变量和承载层样式，壳布局、面板、筛选区、页签、表格、弹窗和卡片统一使用半透明背景、背景模糊、柔和边框和阴影。
- 菜单图标、品牌图标、顶部图标按钮、ratel 助手和语音入口统一增加玻璃图标容器、高光、半透明背景和柔和阴影。

### 2026-07-25：凭证弹窗横向滚动条修正

- 新增和编辑凭证弹窗内分录表压缩列宽并隐藏横向溢出。
- 附件列表移除固定操作列，避免空附件表格出现底部横向滚动条。
- 附件文件名支持换行，避免长文件名撑宽弹窗。
- 全局弹窗表格增加横向溢出兜底，流程定义节点表和流程预览改为自适应显示。

### 2026-07-24：UI 主题和视觉层次优化

- 补齐全局主题变量，包括阴影、焦点环、滚动条、圆角和控件高度。
- 统一 Element Plus 表格、弹窗、弹层、输入控件、按钮、菜单、标签页在不同主题下的表现。
- 优化主壳布局的侧边栏、顶部栏、页签区和内容区视觉层次。
- 壳布局顶部栏和主题、用户、天气按钮支持窄屏适配。
- 登录过期弹窗移除硬编码浅色，改为主题变量。

### 2026-07-23：根目录文档中文化重写

- `README.md` 改为 GitHub 风格中文说明。
- `产品设计.md` 重写为产品设计文档。
- `技术架构.md` 重写为技术架构文档。
- `大模型开发知识点整理.md` 重写为大模型开发知识点。
- `编码打包注释和变更记录.md` 重写为工程规则和变更记录。

### 2026-07-24：根目录文档中英文双版本

- `README.md` 改为英文 GitHub 入口。
- 原中文 `README.md` 改名为 `README.zh.md`。
- 新增 `Product-Design.md`、`Technical-Architecture.md`、`LLM-Development-Guide.md`、`Engineering-Log.md`。
- 中文文档保留并增加英文版链接。

### 2026-07-24：文档同步维护原则

- 增加“中英文文档必须同步更新”的原则。
- 增加“每次改动都需要检查并更新全部根目录 Markdown”的原则。
- 中英文 README、产品设计、技术架构、大模型文档和工程日志均已同步记录该原则。

### 2026-07-24：Ollama 模型状态展示

- AI 组件状态现在展示 Ollama 已配置的聊天、指令、推理、视觉/OCR、Embedding 模型。
- Ollama 组件同时展示 Ollama `/api/tags` 返回的本地已下载模型清单。
- 避免本地存在多个模型或配置了多个场景模型时，页面只看到 `qwen2.5:7b`。

### 2026-07-24：ratel 助手思考中状态

- 浮窗助手初次打开使用更高的稳定面板高度。
- 思考中隐藏输入框和发送按钮。
- 检索和推理过程中只展示 `思考中` 与思考动画。
- 检索完成后恢复 `输入问题` 输入状态和发送按钮。

### 2026-07-24：ratel 助手检索推理审视

- 文件存在性直答现在会在结论中列出命中文件。
- 追问判断现在把总结、归纳、文档内容、上面/上述/前面等识别为依赖上下文的问题。
- 知识检索扩展文档和简历主题词，包括候选人、工程师、工作经历、项目经历、年限、哈尔滨等。
- 用户上传文档在文档、简历、候选人查询中获得文件意图加权。
- 结论和关键依据多条展示时统一使用数字编号，不使用短横线列表。
- 助手流式展示和最终响应都会清理内部推理草稿、复盘语句和 think 标签。
- 新增统一财务专业术语表，供 ratel 助手路由和业务 Agent 选择复用。财务专业问题优先使用实时系统上下文，不再被本地知识库文件召回带偏。
- 架构拆分：新增 `AssistantModelRouter`、`AssistantPromptBuilder`、`AssistantAnswerSanitizer`、`BusinessAgentSelector` 和 `BusinessMetricsService`，降低助手和 Agent 编排服务职责。

### 2026-07-23：业务 Agent 统一入口

- AI 助手页保留 `业务 Agent` Tab。
- 采购、库存、应收应付、会计平台页面移除独立 `Agent 分析` 按钮。
- ratel 助手识别对账、到期、制证建议、库存风险等意图时，通过统一入口承接。
- `FM_AI_AGENT_ENABLED=false` 时，前端隐藏入口，ratel 助手不调用 Agent，后端兜底拦截。

### 2026-07-23：ratel 助手体验修正

- 对话结果只展示结论和关键依据。
- 思考中不再显示额外占位卡片。
- 文件存在性问题优先回答是否有文件。
- 连续追问会结合上一轮主题。

### 2026-07-23：页面问题修正

- 登录密码错误时展示错误提示。
- 智能检索结果区域增加内部滚动，避免底部内容不可见。
- 操作手册内容更新到当前版本。

### 2026-07-23：Linux 部署包补齐

- Ollama 独立包包含 `runtime/linux/ollama/ollama`。
- Qdrant 独立包包含 `runtime/linux/qdrant/qdrant`。
- Ollama Linux 增加 `start.sh`、`stop.sh`、`status.sh`。
- Qdrant Linux 保留 `start.sh`、`stop.sh`、`status.sh`。
- Linux Open WebUI 未内置时只跳过控制台，不影响 Ollama 服务。

## 9. 验证记录

- `frontend` `npm run build`：已通过。
- JDK 24 下 `mvn -DskipTests compile`：已通过。
- 2026-07-23 全量打包已执行，产物如下：
  - `target/ratel-fm.jar`：96.76 MB。
  - `target/ratel-fm-portable.zip`：317.59 MB。
  - `target/ratel-fm-ollama.zip`：22343.69 MB。
  - `target/ratel-fm-qdrant.zip`：64.72 MB。
- ZIP 抽查已确认：
  - Ollama 包包含 `bin/linux/start.sh`、`bin/linux/stop.sh`、`bin/linux/status.sh`、`runtime/linux/ollama/ollama`。
  - Qdrant 包包含 `bin/linux/start.sh`、`bin/linux/stop.sh`、`bin/linux/status.sh`、`runtime/linux/qdrant/qdrant`。

## 2026-07-25 凭证、OCR 与 20 用户三轮回归

- 凭证科目改为层级级联选择，压缩凭证录入列宽，增加非空分录科目及借贷金额逐行校验；在线凭证连续空白金额区按借贷栏各绘制一条斜线。
- OCR 错误区分“能力未配置”和“模型可用但因 CPU、内存、磁盘或超时执行失败”。
- 数据库审计改为有界后台队列落库并同步保留文件审计，消除 20 并发下独立事务争抢连接导致的连接池饥饿。
- Qdrant/Ollama 单条增量索引失败不再回滚采购、库存、应收应付、凭证等核心业务；管理员全量重建仍保留失败反馈。
- 完成 20 在线用户、最近 180 天数据的三轮回归并保留数据库。最终核心回归 780 个请求零失败；深度 AI 测试中智能检索 30/30、Agent 15/15 通过，助手内容质量仅 5/30 通过，已记录为后续高优先级缺陷。

## 2026-07-27 三轮压测、AI 修复与可续跑记录

- 新增根目录 `TEST-PROGRESS.md`，统一记录固定命令、保留库、报告目录、当前进度、失败原因和下一步；AI 用例执行期间每题写入 `progress.json`，中断后可按 offset 接续。
- 修复开发机 `JAVA_HOME` 仍指向 JDK 8 导致的错误编译；工作区统一使用 JDK 24，并为 JDK 24 下的 Mockito 静态 mock 配置显式 Java agent。
- 最终使用不可变 JAR 完成 20 用户、3 轮、180 天、540 组采购/库存/应收应付链路和 180 张借贷平衡草稿凭证回归，共 1,121 请求零失败。
- 压测数据流补充借贷平衡的月末试算草稿凭证，科目从当前账套动态选择启用末级科目；凭证摘要覆盖试算平衡、未过账、跨期、暂估和月末结账，Qdrant 检索探针通过。
- AI 全量每轮覆盖 20 次智能检索、20 次助手问答、10 次专业 Agent 分析和 4 种写作文件。第一轮发现 6 项内容/数据覆盖问题，第二、三轮无 HTTP 或高严重度失败，但分别发现 1、2 项随机内容问题。
- 对制证分录、项目毛利、月末试算与结账、进项发票与税务、专业现金流增加确定性财务护栏；缺少收入成本、发票台账、银行余额等证据时明确说明不可计算，不再用应收应付事实替代，也不允许模型虚构科目编码或借贷结论。
- 最终现金流与发票分片均为 17 请求零失败；后端单测 11/11、前端生产构建通过。
- 本机低配基线：Qdrant 全量 AI 轮次检索 P95 低于 200ms；`qwen2.5:7b` 助手平均约 29 秒、最大约 91 秒；`deepseek-r1:8b` 超过 180 秒，不建议作为该笔记本默认推理模型。
- 本轮未修改 Ollama/Qdrant runtime 或模型文件，按打包原则只生成主 JAR 和 portable ZIP，不重复构建约 22GB 的可选 Ollama 包。
- 曾有一轮在 `spring-boot:run` 直接使用可变 `target/classes` 时与打包/VS Code 编译冲突，导致内部类运行时缺失并产生 60 个无效环境失败；改为正式 JAR 启动后相同并发负载零失败。后续压测禁止在 class 目录服务运行期间并发打包。

## 2026-07-25 ratel 助手深度思考与 AI 写作经营报告回归

- ratel 助手发送意图由平铺按钮改为下拉选择，默认 `思考`，为后续更多意图预留空间。
- `思考` 意图统一传入 `reasoning` 模式，由助手模型路由器选择推理模型场景，避免复杂经营、财务问题落入一般问答模式。
- 思考中展示阶段化过程提示；最终答案返回后自动移除该临时提示，最终回答仍只保留结论和关键依据。
- AI 写作的经营分析报告改为读取 `BusinessMetricsService` 结构化经营指标，覆盖采购金额、往来未结、库存物料、负库存和风险建议，不再生成通用占位文档。
- 新增 `AiWritingServiceTest` 和 `BusinessAgentServiceTest`，覆盖经营报告文件内容、权限边界、经营分析 Agent 指标、风险和自检。
- 本轮只执行定向测试和前端构建，未执行全量打包；原因是改动集中在助手、AI 写作和 Agent 回归，不涉及部署脚本、运行时或装配描述。
- 验证：`npm run build` 通过；`mvn "-Dmaven.compiler.useIncrementalCompilation=false" "-Dtest=AiWritingServiceTest,BusinessAgentServiceTest" test` 通过，3 个测试全部成功。
